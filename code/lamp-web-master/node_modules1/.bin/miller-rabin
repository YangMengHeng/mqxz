#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node --max-old-space-size=3072"  "$basedir/../miller-rabin/bin/miller-rabin" "$@"
  ret=$?
else 
  node --max-old-space-size=3072  "$basedir/../miller-rabin/bin/miller-rabin" "$@"
  ret=$?
fi
exit $ret


